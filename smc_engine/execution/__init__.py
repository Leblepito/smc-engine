"""Sub-proje #5A execution layer — validated_setup → real order pipeline.

Master flag: ``config.execution_enabled`` (default False). False ise bu paket
hiÃ§ instantiate edilmez; runner sub-proje #2 davranıÅı (log-only) korur.
"""
